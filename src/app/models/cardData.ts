export interface CardData {
  airName: string;
  departureDate: string;
  arrivalDate: string;
  departureCountryName: string;
  arrivalCountryName: string;
  refund: boolean;
  direction: boolean;
  totalPrice: string;
}
